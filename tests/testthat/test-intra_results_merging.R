
context("Intra results CNVs merging, combine close events if they share GT value")

# Intricate but rather simpler than the function to combine multiple results
# At the moment has some limitations in the inputs that I will solve ASAP
